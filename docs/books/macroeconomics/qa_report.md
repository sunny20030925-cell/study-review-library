# 《總體經濟學》QA 報告

- 書籍版本：`2026.07.29-1`
- 預定書庫版本：由現行正式版本動態順延；依目前七書 `2026.07.29-8` 計算，中級會計學與本書依序整合後預期為 `2026.07.29-10`
- 檢查日期：2026-07-29

## 第一輪：製作內檢

### 結構與資料

- 正文：20 章。
- 附錄：3 份。
- 題庫：100 題，每章 5 題；題目 ID 全數唯一。
- 搜尋索引：143 筆。
- 自製 SVG：20 張；均具備 `title`、`desc` 與 `viewBox`。
- manifest、questions、search、章節 HTML、SVG 與 service-worker 路徑完成結構檢查。
- 所有題目均具有題幹、答案與必要詳解。

### 內容與公式

逐章檢查定義、符號、模型方向與成立條件，特別覆核：名目／實質變數、GDP 與通膨衡量、Solow 模型、儲蓄投資、消費、投資、貨幣、Keynesian Cross、乘數、IS–LM、AD–AS、預期增廣 Phillips curve、財政與貨幣政策、政府債務動態、匯率與 Mundell–Fleming。

### 製作內檢發現並修正

1. **LaTeX 轉義**：Python 產生器中的 `\frac` 等字串原可能被反斜線跳脫影響；已改用安全字串方式並重新生成，確認正文不含異常 form-feed 或 tab 控制字元。
2. **IS–LM 利率符號條件**：補明短期教材若以 IS 的實質利率 `r` 與 LM 的名目利率 `i` 對照，需假設預期通膨固定或近似不變，才可把兩者變動近似一對一映射。
3. **政府債務動態符號**：將初級盈餘占 GDP 比率明確記為 `ps`，公式寫為 `Δb ≈ (r-g)b-ps`，避免與其他符號混淆。

第一輪結果：通過。

## 第二輪：獨立複核

- 100 題逐題重新檢查題意、答案方向與詳解一致性。
- 辨識出 28 題含數字或量化條件的題目，其中 18 題屬於必須得到具體數值結果的題型，全部獨立重算。
- 18 題數值重算涵蓋成長率、GDP／物價、失業率、成長會計、儲蓄投資、消費、貨幣乘數、數量方程式、支出與稅乘數、政府債務、Taylor 型規則、匯率與開放經濟。
- 重新核對 Solow、IS–LM、AD–AS、Phillips curve、財政政策、貨幣政策與 Mundell–Fleming 的比較靜態方向。
- 重新檢查模型成立條件，避免把固定價格、封閉經濟、完全資本流動或特定匯率制度下的結論誤寫成一般定律。
- 確認正文沒有把 Ramsey、OLG、RBC、New Keynesian 或 DSGE 的正式高階推導納入核心內容。

第二輪結果：通過；18／18 數值題獨立重算一致。

## 整合驗證補強

- 已移除原先由 `validate_cost_accounting.py` 提前生成本書的舊掛接方式。
- 本書改為在既有正式教材與待整合《中級會計學》完成後，於 canonical workflow 最後追加。
- 新 finalizer 依部署當下的正式書庫版本動態順延，不硬編書庫版本。
- 新 validator 會確認所有既有 book ID 仍存在、內容檔可讀，且本書只能追加一次，不得覆蓋或移除既有教材。
- PR gate 會以最新 `main` 組裝完整 workflow，執行 Python 語法、YAML 與關鍵步驟檢查。

## 已知限制

- 本書定位為一般大學總體經濟學，不替代研究所層級的動態總體經濟學。
- 圖形是教材用概念圖，不用於精確讀取未標示的數值。
- 政策章提供模型分析框架，不代表對任何即時政府政策或政治立場作價值判斷。
- 正式完成仍以 canonical GitHub Pages 九書整合驗證成功、部署回條記錄 `book_count=9` 為最後門檻。

## 結論

《總體經濟學》內容包已通過兩輪內容、公式、題庫、數值重算、結構、搜尋與圖解檢查，可進入 PR 整合與正式部署驗證；在 Pages 九書驗證成功前，狀態仍為「待正式部署」。
