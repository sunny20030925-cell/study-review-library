# Mathematical Economics v2 canonical release trigger

This marker intentionally triggers the canonical `Deploy study library` workflow after the validated mathematical-economics v2 deployment step was installed on `main`.
